export const Loading = () => (
    <div className="flex items-center justify-center min-h-screen">
        <div className="spinner-border animate-spin text-blue-600"></div>  {/* Puedes cambiar esto por cualquier spinner */}
    </div>
);

export default Loading;